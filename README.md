# SublimePythonPugin

###  1. python字典对比
`compare_python_dicts.py`
快捷键绑定: 

```json
	{
		"keys": ["ctrl+shift+d"],
		"command": "compare_python_dicts",
		"context": [
			{ "key": "selector", "operator": "equal", "operand": "source.python" }
		]
	}	
```
使用说明:
* 选中同一个文件中的两个字典, 然后按快捷键, 即可对比完成

